# Copy this file to secret.yaml and set a real value before applying.
# secret.yaml is gitignored — never commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: url-short-secret
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme"
